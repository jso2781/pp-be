<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.kids.domain.faq.mapper.FaqMapper">

    <select id="selectFaqList" parameterType="FaqPVO" resultType="FaqRVO" >
        /* 대국민포털_FAQ 목록 조회 (FaqMapper.selectMenuList) */
        SELECT
		    faq_clsf,
		    faq_ttl,
		    faq_seq,
		    lang_se_cd,
		    faq_ans
        FROM "KIDS_OWN"."TB_PP_M_FAQ"
        WHERE 1=1
        AND use_yn = 'Y'
		AND task_se_cd = 'tempTask'
    </select>
</mapper>