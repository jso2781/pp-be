<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.kids.domain.pp.auth.mapper.PicAuthrtMapper">

    <select id="getPicAuthrt" parameterType="PicAuthrtPVO" resultType="PicAuthrtRVO" >
        /* 대국민포털_담당자권한기본 조회 (PicAuthrtMapper.getPicAuthrt) */
        SELECT
             emp_no
            ,authrt_cd
            ,pic_authrt_rmrk
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        FROM kids_own.TB_PP_M_PIC_AUTHRT
        WHERE 1=1
        AND emp_no = #{empNo}
        AND authrt_cd = #{authrtCd}
    </select>

    <insert id="insertPicAuthrt" parameterType="PicAuthrtPVO">
        /* 대국민포털_담당자권한기본 입력 (PicAuthrtMapper.insertPicAuthrt) */
        INSERT INTO kids_own.TB_PP_M_PIC_AUTHRT
        (
             emp_no
            ,authrt_cd
            ,pic_authrt_rmrk
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        )
        VALUES
        (
             #{empNo}
            ,#{authrtCd}
            ,#{picAuthrtRmrk}
            ,#{rgtrId}
            ,now()

            ,#{mdfrId}
            ,now()

        )
    </insert>

    <update id="updatePicAuthrt" parameterType="PicAuthrtPVO">
        /* 대국민포털_담당자권한기본 수정 (PicAuthrtMapper.updatePicAuthrt) */
        UPDATE kids_own.TB_PP_M_PIC_AUTHRT
        SET
             pic_authrt_rmrk = coalesce(#{picAuthrtRmrk}, pic_authrt_rmrk)
            ,mdfr_id = coalesce(#{mdfrId}, mdfr_id)
            ,mdfcn_dt = now()

        WHERE 1=1
        AND emp_no = #{empNo}
        AND authrt_cd = #{authrtCd}
    </update>

    <insert id="savePicAuthrt" parameterType="PicAuthrtPVO">
        /* 대국민포털_담당자권한기본 저장 (PicAuthrtMapper.insertPicAuthrt) */
        INSERT INTO kids_own.TB_PP_M_PIC_AUTHRT
        (
             emp_no
            ,authrt_cd
            ,pic_authrt_rmrk
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        )
        VALUES
        (
             #{empNo}
            ,#{authrtCd}
            ,#{picAuthrtRmrk}
            ,#{rgtrId}
            ,now()

            ,#{mdfrId}
            ,now()

        )
        ON CONFLICT(emp_no,authrt_cd)
        DO UPDATE SET
             pic_authrt_rmrk = coalesce(excluded.pic_authrt_rmrk, kids_own.TB_PP_M_PIC_AUTHRT.pic_authrt_rmrk)
            ,mdfr_id = coalesce(excluded.mdfr_id, kids_own.TB_PP_M_PIC_AUTHRT.mdfr_id)
            ,mdfcn_dt = now()

    </insert>

    <delete id="deletePicAuthrt" parameterType="PicAuthrtDVO">
        /* 대국민포털_담당자권한기본 삭제 (PicAuthrtMapper.deletePicAuthrt) */
        DELETE FROM kids_own.TB_PP_M_PIC_AUTHRT
        WHERE 1=1
        AND emp_no = #{empNo}
        AND authrt_cd = #{authrtCd}
    </delete>

</mapper>