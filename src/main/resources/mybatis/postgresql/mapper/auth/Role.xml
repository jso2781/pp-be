<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.kids.domain.auth.mapper.RoleMapper">

    <select id="getRole" parameterType="RolePVO" resultType="RoleRVO" >
        /* 대국민포털_역할기본 조회 (RoleMapper.getRole) */
        SELECT
             role_cd
            ,role_nm
            ,role_type
            ,role_expln_cn
            ,use_yn
            ,wrtr_dept_nm
            ,mdfr_dept_nm
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        FROM kids_own.TB_PP_M_ROLE
        WHERE 1=1
        AND role_cd = #{roleCd}
    </select>

    <insert id="insertRole" parameterType="RolePVO">
        /* 대국민포털_역할기본 입력 (RoleMapper.insertRole) */
        INSERT INTO kids_own.TB_PP_M_ROLE
        (
             role_cd
            ,role_nm
            ,role_type
            ,role_expln_cn
            ,use_yn
            ,wrtr_dept_nm
            ,mdfr_dept_nm
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        )
        VALUES
        (
             #{roleCd}
            ,#{roleNm}
            ,#{roleType}
            ,#{roleExplnCn}
            ,#{useYn}
            ,#{wrtrDeptNm}
            ,#{mdfrDeptNm}
            ,#{rgtrId}
            ,now()

            ,#{mdfrId}
            ,now()

        )
    </insert>

    <update id="updateRole" parameterType="RolePVO">
        /* 대국민포털_역할기본 수정 (RoleMapper.updateRole) */
        UPDATE kids_own.TB_PP_M_ROLE
        SET
             role_nm = coalesce(#{roleNm}, role_nm)
            ,role_type = coalesce(#{roleType}, role_type)
            ,role_expln_cn = coalesce(#{roleExplnCn}, role_expln_cn)
            ,use_yn = coalesce(#{useYn}, use_yn)
            ,wrtr_dept_nm = coalesce(#{wrtrDeptNm}, wrtr_dept_nm)
            ,mdfr_dept_nm = coalesce(#{mdfrDeptNm}, mdfr_dept_nm)
            ,mdfr_id = coalesce(#{mdfrId}, mdfr_id)
            ,mdfcn_dt = now()

        WHERE 1=1
        AND role_cd = #{roleCd}
    </update>

    <insert id="saveRole" parameterType="RolePVO">
        /* 대국민포털_역할기본 저장 (RoleMapper.insertRole) */
        INSERT INTO kids_own.TB_PP_M_ROLE
        (
             role_cd
            ,role_nm
            ,role_type
            ,role_expln_cn
            ,use_yn
            ,wrtr_dept_nm
            ,mdfr_dept_nm
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        )
        VALUES
        (
             #{roleCd}
            ,#{roleNm}
            ,#{roleType}
            ,#{roleExplnCn}
            ,#{useYn}
            ,#{wrtrDeptNm}
            ,#{mdfrDeptNm}
            ,#{rgtrId}
            ,now()

            ,#{mdfrId}
            ,now()

        )
        ON CONFLICT(role_cd)
        DO UPDATE SET
             role_nm = coalesce(excluded.role_nm, kids_own.TB_PP_M_ROLE.role_nm)
            ,role_type = coalesce(excluded.role_type, kids_own.TB_PP_M_ROLE.role_type)
            ,role_expln_cn = coalesce(excluded.role_expln_cn, kids_own.TB_PP_M_ROLE.role_expln_cn)
            ,use_yn = coalesce(excluded.use_yn, kids_own.TB_PP_M_ROLE.use_yn)
            ,wrtr_dept_nm = coalesce(excluded.wrtr_dept_nm, kids_own.TB_PP_M_ROLE.wrtr_dept_nm)
            ,mdfr_dept_nm = coalesce(excluded.mdfr_dept_nm, kids_own.TB_PP_M_ROLE.mdfr_dept_nm)
            ,mdfr_id = coalesce(excluded.mdfr_id, kids_own.TB_PP_M_ROLE.mdfr_id)
            ,mdfcn_dt = now()

    </insert>

    <delete id="deleteRole" parameterType="RoleDVO">
        /* 대국민포털_역할기본 삭제 (RoleMapper.deleteRole) */
        DELETE FROM kids_own.TB_PP_M_ROLE
        WHERE 1=1
        AND role_cd = #{roleCd}
    </delete>

</mapper>