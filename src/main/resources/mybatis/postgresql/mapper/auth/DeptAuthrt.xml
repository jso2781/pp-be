<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.kids.domain.auth.mapper.DeptAuthrtMapper">

    <select id="getDeptAuthrt" parameterType="DeptAuthrtPVO" resultType="DeptAuthrtRVO" >
        /* 대국민포털_부서권한기본 조회 (DeptAuthrtMapper.getDeptAuthrt) */
        SELECT
             dept_no
            ,authrt_cd
            ,dept_authrt_rmrk
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        FROM kids_own.TB_PP_M_DEPT_AUTHRT
        WHERE 1=1
        AND dept_no = #{deptNo}
        AND authrt_cd = #{authrtCd}
    </select>

    <insert id="insertDeptAuthrt" parameterType="DeptAuthrtPVO">
        /* 대국민포털_부서권한기본 입력 (DeptAuthrtMapper.insertDeptAuthrt) */
        INSERT INTO kids_own.TB_PP_M_DEPT_AUTHRT
        (
             dept_no
            ,authrt_cd
            ,dept_authrt_rmrk
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        )
        VALUES
        (
             #{deptNo}
            ,#{authrtCd}
            ,#{deptAuthrtRmrk}
            ,#{rgtrId}
            ,now()

            ,#{mdfrId}
            ,now()

        )
    </insert>

    <update id="updateDeptAuthrt" parameterType="DeptAuthrtPVO">
        /* 대국민포털_부서권한기본 수정 (DeptAuthrtMapper.updateDeptAuthrt) */
        UPDATE kids_own.TB_PP_M_DEPT_AUTHRT
        SET
             dept_authrt_rmrk = coalesce(#{deptAuthrtRmrk}, dept_authrt_rmrk)
            ,mdfr_id = coalesce(#{mdfrId}, mdfr_id)
            ,mdfcn_dt = now()

        WHERE 1=1
        AND dept_no = #{deptNo}
        AND authrt_cd = #{authrtCd}
    </update>

    <insert id="saveDeptAuthrt" parameterType="DeptAuthrtPVO">
        /* 대국민포털_부서권한기본 저장 (DeptAuthrtMapper.insertDeptAuthrt) */
        INSERT INTO kids_own.TB_PP_M_DEPT_AUTHRT
        (
             dept_no
            ,authrt_cd
            ,dept_authrt_rmrk
            ,rgtr_id
            ,reg_dt

            ,mdfr_id
            ,mdfcn_dt

        )
        VALUES
        (
             #{deptNo}
            ,#{authrtCd}
            ,#{deptAuthrtRmrk}
            ,#{rgtrId}
            ,now()

            ,#{mdfrId}
            ,now()

        )
        ON CONFLICT(dept_no,authrt_cd)
        DO UPDATE SET
             dept_authrt_rmrk = coalesce(excluded.dept_authrt_rmrk, kids_own.TB_PP_M_DEPT_AUTHRT.dept_authrt_rmrk)
            ,mdfr_id = coalesce(excluded.mdfr_id, kids_own.TB_PP_M_DEPT_AUTHRT.mdfr_id)
            ,mdfcn_dt = now()

    </insert>

    <delete id="deleteDeptAuthrt" parameterType="DeptAuthrtDVO">
        /* 대국민포털_부서권한기본 삭제 (DeptAuthrtMapper.deleteDeptAuthrt) */
        DELETE FROM kids_own.TB_PP_M_DEPT_AUTHRT
        WHERE 1=1
        AND dept_no = #{deptNo}
        AND authrt_cd = #{authrtCd}
    </delete>

</mapper>